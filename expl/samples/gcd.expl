int main()
{
decl
    int temp,num,a,b;
enddecl
begin
    temp = exposcall ( "Read" , -1, a );
    temp = exposcall ( "Read" , -1, b );

    while (a!=0) do
    	num=a;
		a=b%a;
		b=num;
    endwhile;
    temp = exposcall ( "Write" , -2, b );
    return 0;
end
}
